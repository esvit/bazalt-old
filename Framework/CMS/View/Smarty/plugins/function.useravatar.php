<?php

function smarty_function_useravatar($params, &$smarty)
{
    /*$avatar = new UserAvatar($params['email']);

    if ($params['size']) {
        $avatar->size($params['size']);
    }

    $avatar->identIcon();

    if ($params['image']) {
        $avatar->image('http://' . $_SERVER['HTTP_HOST'] . $params['image']);
    }


    if ($params['assign']) {
        $smarty->assign($params['assign'], $avatar->getUrl());
    } else {
        return $avatar->getUrl();
    }*/
}