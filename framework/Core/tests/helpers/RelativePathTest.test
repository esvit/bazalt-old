<?php

class RelativePathTest extends Test_BaseCase
{
    public function testPath()
    {
        $fileName = '/test/path';
        $basePath = '/test';

        $this->assertEquals(relativePath($fileName, $basePath), '/path');
    }
}